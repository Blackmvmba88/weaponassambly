# TOKYO Phase II status

Initial deterministic certification slice implemented on branch `feat/tokyo-phase-ii-certification`.

Current scope:

- canonical JSON serialization;
- SHA-256 digest generation;
- build certification record;
- deterministic regression tests;
- architecture note for the certification boundary.

Planned follow-up:

- CLI `certify` command;
- CI golden-fixture comparison;
- optional certification embedding in export metadata.
